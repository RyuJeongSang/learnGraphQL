type Title {
  id: Int!
  title: String!
  rating: Float!
  summary: String!
  language: String!
  medium_cover_image: String!
}

type Query {
  movies(limit: Int, minimum_rating: Float): [Title]!
  movie(id: Int): Title!
  suggestions(id: Int): [Title]!
}
